<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Stock_Market_Simulator.StockSearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Stock_Market_Simulator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <StackPanel Margin="48" Spacing="16">
            <TextBlock x:Name="HeaderTextBlock" Text="Stock Details" FontSize="32" FontWeight="SemiBold"/>

            <Grid x:Name="ResultsGrid" Visibility="Collapsed" Margin="0,24,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="8">
                    <TextBlock x:Name="SymbolTextBlock" FontSize="28" FontWeight="SemiBold"/>
                    <TextBlock x:Name="CompanyNameTextBlock" FontSize="18" Foreground="Gray"/>
                    <TextBlock x:Name="CurrentPriceTextBlock" FontSize="42" Margin="0,12,0,0"/>
                </StackPanel>

                <Grid Grid.Column="1" Margin="48,0,0,0">
                    <lvc:CartesianChart x:Name="PriceChart"/>
                </Grid>

                <ProgressRing x:Name="LoadingRing" IsActive="False" Width="50" Height="50" Margin="0,48,0,0"/>
                <TextBlock x:Name="ErrorTextBlock" Visibility="Collapsed" Text="Stock not found." HorizontalAlignment="Center" Margin="0,48,0,0"/>

            </Grid>
        </StackPanel>
    </Grid>
</Page>
